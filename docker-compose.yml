services:
  web:
    build: .
    container_name: drf_llm_backend
    command: >
      sh -c "python manage.py wait_for_db &&
             python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             gunicorn drf_llm_backend.wsgi:application --bind 0.0.0.0:8000"
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
      - redis

  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"

  db:
    image: postgres:16-alpine
    container_name: postgres_db
    volumes:
      - postgres_db:/var/lib/postgresql/data/
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}

  celery_worker:
    build: .
    container_name: celery_worker
    command: celery -A drf_llm_backend worker --loglevel=info --pool=solo
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
    depends_on:
      - redis
      - db
    env_file:
      - .env

volumes:
  postgres_db: